# sanctifier-cli

`sanctifier-cli` is the command-line interface for Sanctifier, using `sanctifier-core` for static Soroban smart contract analysis.

Install via `cargo install sanctifier-cli` after publishing.
